import java.util.Base64;

public class EmbeddedValues {
    public static void main(string[] args) 
    {
     String encodedPoetry = "RHJ1YXJ5IExvdmU"

     String poetry = new
String(Base64.getDecoder().decode(encodedName));

        System.out.println("Poetry: "+poetry);
    }
}
